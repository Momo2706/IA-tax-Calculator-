class Bracket: 
    def __init__(self, country_id, lower_bound, upper_bound, percentage):
        self.country_id = country_id
        self.lower_bound = lower_bound
        self.upper_bound = upper_bound
        self.percentage = percentage